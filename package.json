{
  "name": "lit-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Live visualization of Claude Code agent activity",
  "scripts": {
    "install:all": "cd server && npm install && cd ../web && npm install",
    "server": "cd server && npm start",
    "web": "cd web && npm run dev",
    "build:web": "cd web && npm run build"
  }
}
